/**
 * 文件职责：组织 Web TypeScript 项目引用。
 * 主要内容：关联浏览器应用与 Node 构建配置两个子项目。
 * 关键边界：只组织编译依赖，具体目标与检查选项由子配置声明。
 */
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
