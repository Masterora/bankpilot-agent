# 当前能力与验证边界

更新：2026-09-21。当前工作树在 `528731f` 上实现 R2 会话与操作结果恢复，尚未提交或发布；详见 [R2 交付记录](R2_ACCEPTANCE.md)。

同日完成深色工作区 UI 重构，设计选择与独立浏览器验收范围见 [UI 重构记录](UI_REFACTOR_2026-09-21.md)。

## 当前能力

- **账户与账单**：注册登录、账户绑定、CSV/XLSX 识别和字段映射、预览、幂等导入、按操作 key 恢复、分类修正、导出及批次撤销。交易保留来源批次与物理行号，多账户歧义需明确选择。
- **交易核对**：重复、本人转账和退款的候选、人工双边搜索、确认、排除及撤销。只有确认关系影响调整统计，金额按币种与交易日期计算；同批重复限制仍由服务端校验。
- **总览与账本**：分币种收支、导入覆盖、最近流水、预算和固定支出待办。总览、账本和规划期间分别管理，URL 支持恢复；批次入口和助手定位不改变总览期间。总览使用 SQL 聚合，关系候选按需检查。
- **预算**：月度分类额度、修改、删除、复制上月缺少项、上月支出参考、覆盖范围、剩余与超支、退款原消费证据；并发冲突保留输入，重新确认后才能保存。
- **固定支出**：月付/年付、月末/闰年日期、下一期预计日期、按生效月份变更、未来配置修改或取消、暂停/恢复/结束；支持流水预填、候选搜索、关联/解除、连续核对及可撤回的“本期未发生”。未关联不等于漏扣。
- **助手与核查**：月度收支、预算、固定支出问答及确认后设置/修改预算。消费与预算共用金额计算，提供逐笔构成、跨月退款原消费、每页 20 条明细、结构化追问和流水定位。账本修订号与 `spending-v1` 计算版本共同保护证据重读，失效返回 409。日期范围核查保留当时快照与运行记录。
- **会话恢复**：服务端保存完整轮次与当时结果；请求去重、90 秒截止收敛、历史分页、范围恢复、删除及无内容去重记录。历史提案读取当前执行资格，确认仍走确定性预算写入与冻结回执。
- **月报**：后台生成、有限重试、历史摘要、更新提示、冻结证据、新旧版本比较、CSV/JSON 导出、打印入口和删除。历史金额不随账本变化重算；比较限历史列表可取得的同月前一成功版本。
- **界面**：全页面深色主题、中英文切换、移动端账本卡片与详情侧栏；宽屏助手并排、窄屏模态显示，切换保留输入。设置页提供账户、主题信息、语言与退出入口。编辑草稿跨页保留，刷新/关闭有未保存提示。
- **运行保障**：统一错误契约、请求与作业耗时记录、3 秒数据库就绪预算、90 秒助手预算及 120 秒 Web 代理等待；离线备份、还原、核对和恢复准备。数据库结构为 `20260921_0013`（20 张表），新增会话和轮次；共享业务库未迁移。

金额、权限、用户隔离与写入校验由服务端确定性代码负责。契约见 [架构](ARCHITECTURE.md)，操作步骤见 [运行手册](RUNBOOK.md)。R1 详见 [设计](R1_DESIGN.md) 与 [交付记录](R1_ACCEPTANCE.md)，页面参考见 [设计图索引](design/dark-theme-2026-09-18/README.md)。

## 验证结果

R2 本地验证与浏览器范围单列于 [交付记录](R2_ACCEPTANCE.md)。下表保留 R1 和基础版本的历史证据，不代表这些旧记录已经覆盖 R2 全部故障矩阵。

| 范围 | 最近验证 | 结果与边界 |
| --- | --- | --- |
| 静态检查与构建 | 2026-09-21 | `make verify`、`git diff --check` 通过 |
| 应用生命周期 | 2026-09-21 | 四组无外部连接回归通过，覆盖正常退出、启动失败、后台任务失败和注入资源所有权；已纳入 `make verify` 与 CI |
| R1 确定性业务回归 | 2026-09-21 | `make verify-business` 在本机随机临时库通过 11 组断言，含迁移与结构检查；结束后清理临时库 |
| 前端定向回归 | 2026-09-21 | `make verify-interactions` 通过 4 组检查；最小 Hook 调度器不替代真实 DOM |
| R1 模型与浏览器 | 2026-09-17 | 模型工具/范围 20/20，文字复核 18/20；浏览器覆盖分页、跨月原单、失效重查及筛选恢复，详见交付记录 |
| 深色页面 | 2026-09-18 | 隔离合成账本验证工作区导航及登录、总览、预算、导入、设置、月报、助手；1440×900 与 390×844 视口覆盖并排/全屏助手、语言切换和预算编辑取消 |

页面验收还覆盖消费 1850、退款 50、实际支出 1800 的五条贡献证据、智能核查记录及月报生成。后续文案精简通过静态检查与构建，未再做完整浏览器验收。业务页面读取实际接口，登录示例卡明确标注为示例。

已记录的基础验收（2026-09-15 至 09-17，隔离库与合成账户）：

- 导入并发幂等、摘要/编号冲突、拒绝计数、撤销重放、来源解析及用户隔离；页面完成预览—导入—历史—批次流水，并验证提交等待时刷新恢复。
- 预算退款归属、币种隔离、复制不覆盖及版本冲突；周期日期锚点、历史保护、未来变更、关联互斥、未发生确认/撤回及撤销导入联动。
- 连续操作验证总览与账本范围独立、关系确认/撤销、筛选移除、固定支出连续核对、预算草稿往返、月报冻结证据与版本比较；包含 390×844 视口。
- 提案无写入、并发确认一次生效、取消/到期/版本冲突、冻结回执、事务回滚和超时；错误契约覆盖认证、参数、404、数据库不可用及内部错误，敏感输入不回显。
- 空库迁移及降级再升级、18 表备份还原、结构差异检测、冲突连接配置拒绝和重复恢复准备；中断任务覆盖重排队、失败与未知状态，旧令牌/会话失效。

基础验收包含仓库外临时脚本，不等同于当前自动回归完整覆盖。固定脚本及运行要求见 [验收说明](../scripts/acceptance/README.md)。CI 已配置确定性回归，本地通过不代表远程 CI 或生产验收通过。

## 性能证据

2026-09-15 本地 HTTP，每组预热 10 次、测量 100 次；未加入大量历史关系，未测 SSH/Tailscale 链路：

| 期间交易数 | 并发 | P50 | P95 | 返回体 |
| --- | --- | --- | --- | --- |
| 1,000 | 1 | 9.37 ms | 10.25 ms | 2,080 B |
| 1,000 | 5 | 18.33 ms | 24.00 ms | 2,080 B |
| 10,000 | 1 | 22.76 ms | 23.59 ms | 2,088 B |
| 10,000 | 5 | 28.94 ms | 35.16 ms | 2,088 B |


临时小数据备份约 147 ms、还原与准备约 203 ms，仅为该次演练规模，不代表生产 RPO/RTO。未通过新增缓存、扩大连接池或放宽容量上限获取结果。

2026-09-18 本地 API 经 SSH/Tailscale 连接远程数据库的三次诊断：页面 HTML 1–8 ms，无数据库健康接口 1–2 ms，执行 `SELECT 1` 的就绪接口 1.34–1.59 s；Tailscale 经香港 DERP 中继，往返 252–267 ms，未建立直连。这是当次观测；API 与数据库同机或同内网、改善直连均尚未作为本轮优化落地。

## 未完成与限制

- 微信/支付宝真实脱敏导出样本认证未完成；合成验收不能证明所有格式兼容，无编号账单的跨文件身份歧义仍存在。
- 候选扩展窗口超过 10,000 笔时停止发现，仍读取完整期间统计和已保存关系；期间本身超限则拒绝查询，不返回截断合计。拆分转账、汇兑及手续费差额不支持自动配对。
- 任意自然语言流水搜索、自动周期识别、外部提醒、预算结转、共享预算和自助注销未交付。周期暂停不保存历史暂停区间。
- 模型文字仍可能混淆无导入数据与零消费、贡献条数与购买次数；确定性金额卡和证据为准。20 例结果不能代表总体模型准确率。
- 真机、弱网、原生月份弹层、系统打印/PDF 分页及生产代理专项验收未完成；助手预算确认的完整浏览器流程未记录为通过。
- 月报删除与完成竞争、租约耗尽及数据库短时中断的完整矩阵待补齐；加密存储、异机备份、保留策略与代表性恢复目标仍需实际环境确认。
- 大量历史关系与远程链路的 P50/P95、连接等待和 SQL 计划仍需专项测量。
- 生产发布需前后端同批部署、旧协议/规则版本拒绝与线上验收；推送 `main` 可能触发部署。后续功能按 [路线图](ROADMAP.md) 推进。

系统不登录网银、不读取实时余额、不执行资金操作；未导入不代表无消费，净流入不代表余额。
