# 当前交付状态

## 已交付

| 模块 | 已实现闭环 |
| --- | --- |
| 身份 | 注册、登录、退出、Session 与用户归属隔离 |
| 导入 | UTF-8/GB18030 CSV 与单表 XLSX；标准字段、支付宝及微信字段结构识别；账户确认、预览、原子校验、去重、冲突拒绝、排除报告、撤销与重导 |
| 识别恢复 | 区分认证、接口缺失、文件校验、限流和网络错误；服务故障后可原文件重试，文件仅留在组件内存 |
| 解析 | 小数点金额、拒绝分组符与歧义金额；记录日期、时间或未知精度 |
| 账本 | 期间查询、账户/商户筛选、来源批次与行号、分类保存、筛选结果 CSV 导出 |
| 核查 | 大额流出与有时间证据的疑似重复；待处理、正常、待核实和备注持久化；可恢复待处理 |
| Agent | OpenRouter 规划单次日期查询；确定性分类、按币种流水统计与规则证据 |
| 历史 | 当前用户最近 50 次运行、结果快照与运行事件；刷新后可重新选择历史 |
| 结果一致性 | 分类修正不覆盖生成时结果；新查询读取修正后的账本 |
| Web | React、深色主题、中英切换；说明默认折叠；时间按 UTC+8 展示到秒，日期或未知精度明确标识 |

## 验收证据

| 检查 | 结果 |
| --- | --- |
| API | Ruff、Mypy；真实接口验收覆盖注册登录、导入、拒绝、去重、分类、核查、撤销重导与用户隔离 |
| Web | ESLint、TypeScript 与 Vite 构建；页面验收覆盖登录、导入预览与确认、筛选、备注保存、导出、语言切换和 Agent 查询 |
| CSV | 模拟流水去重后 19 笔，净额 11775.05 CNY；无时区时间戳拒绝、空格表头与超长字段已验证 |
| 核查 | 结论保存与恢复、跨用户隔离、交易撤销后不能提交旧证据 |
| 迁移 | 远程 PostgreSQL 已为 20260905_0005；本地执行 upgrade head 无待迁移，alembic check 无结构差异 |
| 本地运行 | React :5173 → 本地 API :8000 → Tailscale SSH 隧道 → 远程 PostgreSQL；API 与代理就绪检查通过 |
| 隔离迁移 | 临时独立 PostgreSQL 从空库 upgrade head、alembic check 均通过，验收库已删除 |
| 来源解析 | 公开字段结构构造数据：微信 3 笔、排除 2 行；支付宝 2 笔，合计流水净额 75 CNY；跨来源编号、重叠导入、XLSX/CSV 去重、未知状态拒绝、金额冲突和撤销重导通过 |
| 来源页面 | 微信结构文件识别、账户确认、秒级时间、预览和排除数量、确认导入与历史报告通过 |
| 数据边界 | 真实 PostgreSQL 使用独立验收账户；验收后清理账号与关联数据，不保留测试文件 |
| Agent 恢复 | PostgreSQL 验证心跳续约、活跃任务保留和超时收敛；真实 OpenRouter 查询成功 |
| CI | 仅静态检查、构建与隔离 PostgreSQL 迁移检查，不提供自动回归保障 |

## 支持边界

- CSV/XLSX 最多 10 MB、5,000 条交易；标准字段金额用小数点与正负号；来源解析器按明确方向转换，不猜测小数逗号。
- 支付宝、微信解析器已实现并以公开结构构造数据验收，尚无真实导出脱敏样本验证，不承诺平台所有格式兼容。XLSX 限未加密单表，不允许公式或已丢精度的长数字编号；PDF、XLS 与压缩包不支持。
- 明确失败和中性记录逐行报告；未知状态、方向歧义和无法确定的退款拒绝。来源账户名称由用户确认，不代表平台身份认证；同一来源使用不同名称会建立不同账户。
- 汇总为流入、流出和流水净额，尚未抵销本人转账、还款、重复来源与退款；不称为净消费。
- 期间完整性未验证；没有记录不等于没有消费，首末笔日期不等于账单覆盖范围。
- 核查结论是用户判断，不删除交易、不关闭银行扣款、不确认实际退款；结论保存最新状态与备注，不提供结论修改历史。
- 核查证据使用交易 ID；撤销重导产生新交易后需要重新核查，不自动继承旧判断。
- CSV 导出用于阅读核对，不是原账单或无损备份，不承诺重新导入去重。
- 历史列表最多 50 条；源行与批次可追溯，但尚无完整来源记录版本和账本版本管理。
- 周期扣款、预算为空态；多工具核查、交易关联与正式月度报告见路线图。
- 用户输入会发送至模型供应商；不要在核查任务中粘贴密钥或敏感原文。

## 文档入口

本轮本地功能与验收完成；提交、推送、CI、远端发布及发布后验收分别处理，不计入本地功能完成条件。真实文件兼容认证仍需实际脱敏账单。

[当前任务与后续计划](ROADMAP.md) · [运行与接口](RUNBOOK.md)
