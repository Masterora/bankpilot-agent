# 文件职责：组装 BankPilot K3s 默认部署所需的声明式资源。
# 主要内容：命名空间、公开配置、PostgreSQL、API 和 Web/Ingress。
# 关键边界：环境特定的 DNS 兼容配置不进入默认资源集，密钥由部署脚本动态创建。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - api.yaml
  - web.yaml
