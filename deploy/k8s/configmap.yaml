# 文件职责：定义 K3s 中可公开的 BankPilot API 运行配置。
# 主要内容：环境、会话、模型端点、重试、Token 上限与数据收集策略。
# 关键边界：本 ConfigMap 不存储数据库密码、会话密钥、模型 ID 或 API Key。
apiVersion: v1
kind: ConfigMap
metadata:
  name: bankpilot-config
  namespace: bankpilot
data:
  BANKPILOT_ENV: production
  BANKPILOT_SESSION_TTL_SECONDS: "28800"
  BANKPILOT_SESSION_COOKIE_SECURE: "false"
  BANKPILOT_CORS_ORIGINS: ""
  MODEL_PROVIDER: openrouter
  MODEL_BASE_URL: https://openrouter.ai/api/v1
  MODEL_TIMEOUT_SECONDS: "30"
  MODEL_MAX_RETRIES: "1"
  MODEL_MAX_TOKENS: "1600"
  MODEL_REASONING_EFFORT: low
  MODEL_REQUIRE_PARAMETERS: "true"
  MODEL_DATA_COLLECTION: deny
